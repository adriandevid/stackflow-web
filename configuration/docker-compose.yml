
services:


#start registry
  registry: 
    image: registry:3 
    container_name: registry 
    entrypoint:  ./registry-init.sh 
    restart: always 
    ports: 
      - 5000:5000 
    volumes: 
      - ./auth/:/auth 

#end registry
#[content]
        
networks:
  web:
    external: false
